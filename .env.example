# Deployment Environment Variables
# For Render, you can specify these in your environment tab.

# Set to 'production' to disable debug mode in Flask
FLASK_ENV=production

# Render automatically sets PORT, but you can override it locally
PORT=10000

# (Optional) If you later add a database, you would add the URL here
# DATABASE_URL=postgres://user:pass@host:port/dbname
